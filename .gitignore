_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
/vendor/

# Firebase config is generated at build time from GitHub Secrets — never commit it
assets/firebase-config.js
.DS_Store

# Material-fonte usado para gerar o conteúdo do site /china/ — não publicar
china/china-repositorio/
